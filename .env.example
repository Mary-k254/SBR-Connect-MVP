# Smart Ride Connect - Environment Variables

# =====================================================
# DATABASE CONFIGURATION (Required for Production)
# =====================================================

# Option 1: Vercel Postgres (Recommended)
# Create at: https://vercel.com/dashboard → Storage → Create Database → Postgres
# Vercel automatically sets POSTGRES_URL as an environment variable

# Option 2: Supabase PostgreSQL
# Get connection string from: https://supabase.com/dashboard → Your Project → Settings → Database
# Connection string format: postgres://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres

# For local development, leave POSTGRES_URL empty to use SQLite
# POSTGRES_URL=postgres://user:password@host:5432/database

# Alternative: Individual PostgreSQL parameters (if not using POSTGRES_URL)
# POSTGRES_HOST=your-postgres-host
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-password
# POSTGRES_DB=postgres

# =====================================================
# AUTHENTICATION
# =====================================================

# JWT Secret - Generate a secure random string for production
# Run: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-key-change-in-production

# =====================================================
# API CONFIGURATION
# =====================================================

NODE_ENV=development
API_URL=http://localhost:3000
API_RATE_LIMIT=100

# =====================================================
# M-PESA CONFIGURATION (Optional - for mobile payments)
# =====================================================

# Mpesa_CONSUMER_KEY=your-mpesa-consumer-key
# Mpesa_CONSUMER_SECRET=your-mpesa-consumer-secret
# Mpesa_SHORTCODE=your-mpesa-shortcode
# Mpesa_PASSKEY=your-mpesa-passkey
# Mpesa_ENV=development

# =====================================================
# EXTERNAL APIs (Optional)
# =====================================================

# MAPS_API_KEY=your-google-maps-api-key
# SMS_API_KEY=your-sms-api-key
